# Basic Scanner Tool

## 📌 Description
Basic Scanner is a simple Python-based network scanning tool developed for educational and practice purposes.  
The tool allows users to scan a specific port on a target IP address by providing the required command-line arguments.

It is designed to demonstrate basic concepts of:
- Network scanning
- Python scripting
- Command-line argument handling

---

## 🚀 Usage Instructions

### Command Format
The tool follows a straightforward command structure:

```bash
python3 <tool_name>.py <port_number> <ip_address>
python3 basic_scaner.py 80 192.168.1.10
